<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= note.noteTitle %> - Accela Notes</title>
  <link rel="stylesheet" href="/chapters.css">
  <link rel="icon" href="https://files.catbox.moe/bpi1je.gif" type="image/gif">
  <style>
    .breadcrumb {
      max-width: 1200px;
      margin: 20px auto 0;
      padding: 0 20px;
      display: flex;
      align-items: center;
      gap: 8px;
      font-size: 14px;
      color: rgba(255, 255, 255, 0.6);
      flex-wrap: wrap;
    }

    .breadcrumb a {
      color: #4f46e5;
      text-decoration: none;
      transition: color 0.2s;
    }

    .breadcrumb a:hover {
      color: #6366f1;
    }

    .breadcrumb-separator {
      color: rgba(255, 255, 255, 0.3);
    }

    .note-container {
      max-width: 1200px;
      margin: 20px auto;
      padding: 0 20px;
    }

    .note-wrapper {
      background: white;
      border-radius: 16px;
      overflow: hidden;
      box-shadow: 0 8px 24px rgba(0, 0, 0, 0.3);
    }

    .note-content {
      /* Reset any conflicting styles for the HTML note */
      all: initial;
      display: block;
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', 'Roboto', 'Oxygen', 'Ubuntu', sans-serif;
    }

    @media (max-width: 768px) {
      .breadcrumb {
        margin-top: 12px;
      }
    }
  </style>
</head>
<body>

  <!-- Header -->
  <header class="header">
    <div class="logo-title">
      <img src="https://files.catbox.moe/bpi1je.gif" alt="Animated Accela Logo" class="logo" />
      <h1 id="accela">Accela</h1>
    </div>
  </header>

  <!-- Breadcrumb Navigation -->
  <nav class="breadcrumb">
    <a href="/">🏠 Home</a>
    <span class="breadcrumb-separator">/</span>
    <a href="/notes">Notes</a>
    <span class="breadcrumb-separator">/</span>
    <a href="/notes/chapter/<%= note.chapterIndex %>">Chapter <%= note.chapterIndex %></a>
    <span class="breadcrumb-separator">/</span>
    <span>Topic <%= note.topicIndex %></span>
  </nav>

  <!-- Note Content -->
  <div class="note-container">
    <div class="note-wrapper">
      <div class="note-content">
        <%- note.htmlContent %>
      </div>
    </div>
  </div>

  <!-- Buttons Container -->
  <div class="buttons-container">
    <div class="button-row">
      <a href="/notes/chapter/<%= note.chapterIndex %>" class="admin-btn">← Back to Topics</a>
      <% if (isAuthenticated) { %>
        <form action="/logout" method="GET">
          <button type="submit" class="logout-btn">Logout</button>
        </form>
      <% } else { %>
        <a href="/login" class="admin-btn">Login / Sign Up</a>
      <% } %>
    </div>
  </div>

</body>
</html>
