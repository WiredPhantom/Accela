<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Home - Accela</title>
  <link rel="stylesheet" href="/chapters.css">
  <link rel="icon" href="https://files.catbox.moe/bpi1je.gif" type="image/gif">
  <style>
    .user-status {
      position: absolute;
      top: 20px;
      right: 40px;
      background: rgba(255, 255, 255, 0.05);
      padding: 8px 16px;
      border-radius: 8px;
      font-size: 14px;
      color: rgba(255, 255, 255, 0.7);
    }

    .user-status.premium {
      background: rgba(255, 215, 0, 0.1);
      border: 1px solid rgba(255, 215, 0, 0.3);
      color: #ffd700;
    }

    .home-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
      gap: 24px;
      max-width: 900px;
      margin: 40px auto;
      padding: 0 20px;
    }

    .home-card {
      background: rgba(255, 255, 255, 0.05);
      border: 1px solid rgba(255, 255, 255, 0.1);
      border-radius: 16px;
      padding: 40px 32px;
      text-align: center;
      transition: all 0.3s;
      cursor: pointer;
      text-decoration: none;
      color: inherit;
      display: block;
    }

    .home-card:hover {
      background: rgba(255, 255, 255, 0.08);
      transform: translateY(-4px);
      box-shadow: 0 12px 32px rgba(0, 0, 0, 0.4);
      border-color: rgba(255, 255, 255, 0.2);
    }

    .home-card-icon {
      font-size: 64px;
      margin-bottom: 20px;
      display: block;
    }

    .home-card h2 {
      font-size: 28px;
      font-weight: 700;
      margin-bottom: 12px;
      color: #ffffff;
    }

    .home-card p {
      font-size: 16px;
      color: rgba(255, 255, 255, 0.7);
      line-height: 1.6;
    }

    .home-card.coming-soon {
      opacity: 0.6;
      cursor: not-allowed;
    }

    .home-card.coming-soon:hover {
      transform: none;
      box-shadow: none;
    }

    .coming-soon-badge {
      display: inline-block;
      background: rgba(255, 255, 255, 0.1);
      color: rgba(255, 255, 255, 0.6);
      padding: 4px 12px;
      border-radius: 8px;
      font-size: 12px;
      font-weight: 600;
      margin-top: 12px;
    }

    @media (max-width: 768px) {
      .user-status {
        position: static;
        margin: 16px auto;
        text-align: center;
        max-width: 200px;
      }

      .home-grid {
        grid-template-columns: 1fr;
        gap: 16px;
      }

      .home-card {
        padding: 32px 24px;
      }

      .home-card-icon {
        font-size: 48px;
      }
    }
  </style>
</head>
<body>

  <!-- Header -->
  <header class="header" style="position: relative;">
    <div class="logo-title">
      <img src="https://files.catbox.moe/bpi1je.gif" alt="Animated Accela Logo" class="logo" />
      <h1 id="accela">Accela</h1>
    </div>
    
    <% if (isAuthenticated) { %>
      <div class="user-status <%= hasPremium ? 'premium' : '' %>">
        <% if (hasPremium) { %>
          ⭐ Premium User
        <% } else { %>
          👤 <%= user.username %>
        <% } %>
      </div>
    <% } %>
  </header>

  <!-- Home Grid -->
  <div class="home-grid">
    <!-- Notes Card (Coming Soon) -->
    <div class="home-card coming-soon">
      <span class="home-card-icon">📝</span>
      <h2>Notes</h2>
      <p>Comprehensive study notes organized by chapters and topics</p>
      <span class="coming-soon-badge">Coming Soon</span>
    </div>

    <!-- Flashcards Card (Active) -->
    <a href="/chapters" class="home-card">
      <span class="home-card-icon">🗂️</span>
      <h2>Flashcards</h2>
      <p>Interactive flashcards for active recall and spaced repetition</p>
    </a>
  </div>

  <!-- About the Site -->
  <section id="lain" class="section lain-section">
    <h2 id="aboutsite">About the Site</h2>
    <div class="lain-container">
      <img src="https://files.catbox.moe/bpi1je.gif" alt="Glitched image of Lain representing site ethos" class="lain-img" />
      <p class="text">
        ACCELA was not born from inspiration — it was born from necessity. There wasn't a tool aligned with the structure that was needed. So the webmaster stopped waiting. And started building.
        <br><br>
        <strong>🚀 Currently: Core topics are <span style="color: #00ffcc;">free to access</span>.</strong><br>
        <strong>🔒 Premium content unlocks advanced chapters and topics.</strong><br>
        <em>You're here early — explore what resonates.</em><br>
        <br>
        This is not a traditional study site. It's a system shaped around <em>Active Recall</em>. You won't remember what you just read. You'll remember what your memory was forced to retrieve. That's the rule — we remember what we remembered, not what we read.
        <br><br>
        There's a Cbox. It's more than a chat — it's a bridge. Questions, suggestion, appreciation — everything is signal. And all signal reaches the webmaster.
        <br><br>
        You've entered a space shaped by need, refined by memory, and held together by obsession.  
        <br><br>
        Thank you for entering. If you have something to say, leave a trace in the Cbox.
      </p>
    </div>
  </section>

  <!-- Cbox -->
  <section id="cbox" class="section">
    <h2>Cbox</h2>
    <iframe src="https://www5.cbox.ws/box/?boxid=957561&boxtag=IZAjsP"
      allow="autoplay" frameborder="0" marginheight="0" marginwidth="0" scrolling="auto"></iframe>	
  </section>

  <!-- Updates -->
  <section id="updates" class="section">
    <h2 id="siteupdate">Site Updates</h2>
    <ul class="updates">
      <li>🏠 New home page with Notes and Flashcards sections!</li>
      <li>✨ Freemium model launched — Free and Premium content available!</li>
      <li>🔒 Premium chapters now marked with lock icons.</li>
      <li>👤 User authentication updated for better experience.</li>
      <li>📚 Chapters page refined — cleaner flow and better navigation.</li>
    </ul>
  </section>

  <!-- Buttons Container -->
  <div class="buttons-container">
    <div class="button-row">
      <% if (isAuthenticated) { %>
        <form action="/logout" method="GET">
          <button type="submit" class="logout-btn">Logout</button>
        </form>
        <% if (user && user.role === 'admin') { %>
          <a href="/admin" class="admin-btn">Admin Panel</a>
        <% } %>
      <% } else { %>
        <a href="/login" class="admin-btn">Login / Sign Up</a>
      <% } %>
    </div>
  </div>

</body>
</html>
